{
  "name": "eventos-worker",
  "version": "1.0.0",
  "description": "EventOS Cloudflare Worker - D1 + R2 Backend",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute eventos-db --file=schema.sql"
  }
}
